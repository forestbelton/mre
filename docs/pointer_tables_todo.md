# Pointer-table worklist

_Auto-generated by `tools/find_pointer_tables.py` (heuristic; eyeball before converting). Run it again to refresh after symbolizing tables._

Found **11** candidate pointer-table runs (**0** still raw `db`, actionable). Far runs -> `farptr`; near `dw` runs -> `dw Label`. Targets shown as `bank:addr` -> resolved `label[+offset]` (blank = no nearby label).

## Actionable (raw `db`/`dw $hex`)

## Already symbolic (9) — section uses farptr/dw Label

- $01:$523a 36x near/dw `Items` (item.asm)
- $00:$39ee 19x near/dw `ScriptEngine` (engine.asm)
- $03:$4098 17x near/dw `Room engine` (engine.asm)
- $05:$461a 16x near/dw `analyzed_0142a8` (scene.asm)
- $10:$408c 12x near/dw `analyzed_040000` (bg_loader.asm)
- $03:$40d8 9x near/dw `Room engine` (engine.asm)
- $00:$3b73 8x near/dw `ScriptEngine` (engine.asm)
- $03:$40c4 8x near/dw `Room engine` (engine.asm)
- $00:$0f83 4x far/bank_first `analyzed_000f7d` (home.asm)

## Generated, not loose db (2) — macro/INCBIN section (e.g. SOUND_DRIVER); table bytes aren't editable here

- $3e:$454f 12x near/dw `Sound bank 1` (bank1.asm)
- $3f:$454f 12x near/dw `Sound bank 0` (bank0.asm)
